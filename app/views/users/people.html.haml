%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - @user.follows.each do |follow|
        %tr
          %td
            %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest('knwang@gmail.com'.downcase)}?s=40")
            = link_to follow.following.username, user_path(follow.following)
          %td.extra-padding #{follow.following.queue_items.count}
          %td.extra-padding #{follow.following.followers.count}
          %td.extra-padding
            = link_to user_follow_path(@user,follow), method: :delete do
              %i.glyphicon.glyphicon-remove
